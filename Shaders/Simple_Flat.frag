#version 450

layout(location = 0) out vec4 outColor;   // Final output color

void main() {
    // Hardcoded flat color (e.g., red)
    outColor = vec4(1.0, 0.0, 0.0, 1.0); // RGBA (Red, Green, Blue, Alpha)
}